# VAS

## Install
```
composer install
cp .env.example .env

# edit .env file

php artisan k:g

# create database
# mysql -u root -p -e "create database db_name"

php artisan m:fr --seed

# start the server 
sudo ./startup.sh
```
